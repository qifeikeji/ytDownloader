name: Publish to WinGet (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow has been disabled."
          echo "Reason: we only build Linux AppImage and upload it as a workflow artifact (no GitHub Release publishing)."
